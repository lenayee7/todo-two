$(document).ready(function() {
    // add on click
    // jQuery .on()
    $('.dot').on('click', function(event){
      // get data color value from event target
      var selectedColorValue = $(event.target).data('color-value');
      // find color changing element
      // add class to color changing element
    });
});
